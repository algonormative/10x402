# Check candidates — harvested, not yet judged

Format and rules: [README.md](README.md). Entries are appended by the
`x402-field-harvest` routine (≤3/tick) and by interactive sessions; statuses
move only in human sessions. Nothing here is a check until an engine session
implements it against the calibration suite and the self-lint gate.

The pre-routine harvest — wrong-bag, the negative-control pair, the
#3045-derived bazaar checks — shipped straight to the engine during
interactive sessions 2026-08-19 → 2026-08-21 and is recorded in git history
and `audit/2026-08-19/` rather than retro-filed here. This file starts empty
on purpose: its first entries should be the routine's own.
